<Window x:Class="EMR.Views.AssignPatientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EMR.Views"
        xmlns:vm="clr-namespace:EMR.ViewModels"
        mc:Ignorable="d"
        Title="AssignPatientWindow" Height="650" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Window.DataContext>
        <vm:AssignPatientViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🏥 Assign Patient to Caretaker" FontSize="24" FontWeight="Bold" Foreground="#2196F3"/>
            <TextBlock Text="Create a new assignment between patient and caretaker" 
                      FontSize="13" Foreground="#666" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Select Patient *" Style="{StaticResource LabelStyle}"/>
                <ComboBox ItemsSource="{Binding Patients}" 
                          SelectedItem="{Binding SelectedPatient}"
                          DisplayMemberPath="FullName"
                          Style="{StaticResource ComboBoxStyle}"/>

                <TextBlock Text="Select Caretaker *" Style="{StaticResource LabelStyle}"/>
                <ComboBox ItemsSource="{Binding Caretakers}" 
                          SelectedItem="{Binding SelectedCaretaker}"
                          DisplayMemberPath="FullName"
                          Style="{StaticResource ComboBoxStyle}"/>

                <TextBlock Text="Assignment Date *" Style="{StaticResource LabelStyle}"/>
                <DatePicker SelectedDate="{Binding AssignedDate}" 
                           Height="40" FontSize="14" Padding="10"/>


                <TextBlock Text="Ending Date *" Style="{StaticResource LabelStyle}"/>
                <DatePicker SelectedDate="{Binding EndDate}" 
           Height="40" FontSize="14" Padding="10"/>

                <TextBlock Text="Notes (Optional)" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource TextBoxStyle}"
                         Height="100"/>

                <!-- Patient Info Display -->
                <Border Background="#E3F2FD" 
                        BorderBrush="#2196F3" 
                        BorderThickness="0,0,0,2"
                        CornerRadius="4"
                        Padding="15"
                        Margin="0,20,0,0"
                        Visibility="{Binding ShowPatientInfo, Converter={StaticResource BoolToVisConverter}}">
                    <StackPanel>
                        <TextBlock Text="Patient Information" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,10"/>
                        <TextBlock>
                            <Run Text="Name: " FontWeight="SemiBold"/>
                            <Run Text="{Binding SelectedPatient.FullName, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Margin="0,5,0,0">
                            <Run Text="Age: " FontWeight="SemiBold"/>
                            <Run Text="{Binding SelectedPatient.Age, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Margin="0,5,0,0">
                            <Run Text="Blood Group: " FontWeight="SemiBold"/>
                            <Run Text="{Binding SelectedPatient.BloodGroup, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Caretaker Info Display -->
                <Border Background="#E8F5E9" 
                        BorderBrush="#4CAF50" 
                        BorderThickness="0,0,0,2"
                        CornerRadius="4"
                        Padding="15"
                        Margin="0,10,0,0"
                        Visibility="{Binding ShowCaretakerInfo, Converter={StaticResource BoolToVisConverter}}">
                    <StackPanel>
                        <TextBlock Text="Caretaker Information" FontWeight="Bold" Foreground="#4CAF50" Margin="0,0,0,10"/>
                        <TextBlock>
                            <Run Text="Name: " FontWeight="SemiBold"/>
                            <Run Text="{Binding SelectedCaretaker.FullName, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Margin="0,5,0,0">
                            <Run Text="Specialization: " FontWeight="SemiBold"/>
                            <Run Text="{Binding SelectedCaretaker.Specialization, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Margin="0,5,0,0">
                            <Run Text="Shift: " FontWeight="SemiBold"/>
                            <Run Text="{Binding SelectedCaretaker.Shift, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" 
                          Foreground="#F44336" 
                          Margin="0,15,0,0" 
                          TextWrapping="Wrap"
                          Visibility="{Binding HasError, Converter={StaticResource BoolToVisConverter}}"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Assign" 
                    Command="{Binding AssignCommand}"
                    Style="{StaticResource SuccessButtonStyle}"
                    Width="120" Margin="0,0,10,0"/>

            <Button Grid.Column="2" Content="Cancel" 
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Width="120"/>
        </Grid>
    </Grid>
</Window>
